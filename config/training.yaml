data:
  data_dir: /content/data/temporal
  node_counts:
    brand: 2734
    category: 14
    product: 60798
    user: 267973
  struct_dir: /content/data/temporal/node_mappings

model:
  dropout: 0.2
  embed_dim: 128
  n_layers: 2
  rank: 32
  use_grad_checkpoint: false

sampler:
  hop1_budget: 10
  hop2_budget: 5
  hop1_sample_replace: true

training:
  amp: true
  use_bf16: true
  batch_size: 8192
  device: cuda
  epochs: 30
  eval_batch_size: 16384
  eval_every: 1
  l2_lambda: 1.0e-06
  lr: 0.0003
  max_grad_norm: 1.0
  cl_weight: 0.05
  max_view_triplets: 4000000
  num_neg: 8
  num_workers: 4
  patience: 5
  save_dir: /content/checkpoints-v3
  weight_decay: 1.0e-03

wandb:
  artifact_name: bagnn-checkpoint-v3
  enabled: true
  entity: nguyenmaiductrong37-h-c-vi-n-c-ng-ngh-b-u-ch-nh-vi-n-th-ng
  project: bagnn-recsys
  run_name: bagnn-g4-v3
  save_every: 1