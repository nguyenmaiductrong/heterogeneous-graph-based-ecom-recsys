data:
  data_dir: /content/data/temporal
  node_counts:
    brand: 2734
    category: 14
    product: 60798
    user: 267973
  struct_dir: /content/data/temporal/node_mappings

model:
  dropout: 0.4          # [Đã sửa] Tăng từ 0.1 -> 0.4
  embed_dim: 64         # [Đã sửa] Giảm từ 128 -> 64
  n_layers: 2
  rank: 16

sampler:
  hop1_budget: 15
  hop2_budget: 10

training:
  amp: true
  batch_size: 2048  
  device: cuda
  epochs: 30
  eval_batch_size: 16384 
  eval_every: 1
  l2_lambda: 1.0e-04 
  lr: 0.0003
  max_grad_norm: 1.0
  num_neg: 1
  num_workers: 8
  patience: 5
  save_dir: /content/checkpoints-c2
  weight_decay: 1.0e-03

wandb:
  artifact_name: bagnn-checkpoint-v2
  enabled: true
  entity: nguyenmaiductrong37-h-c-vi-n-c-ng-ngh-b-u-ch-nh-vi-n-th-ng
  project: bagnn-recsys
  run_name: bagnn-g4-v2
  save_every: 1