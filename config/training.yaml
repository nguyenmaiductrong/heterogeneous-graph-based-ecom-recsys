data:
  data_dir: "data/processed/temporal"
  struct_dir: "data/processed/temporal/node_mappings"
  node_counts:
    user: 267973
    product: 60798
    category: 14
    brand: 2734

model:
  embed_dim: 128
  n_layers: 2
  rank: 16
  dropout: 0.1

sampler:
  hop1_budget: 15  
  hop2_budget: 10  

training:
  epochs: 30
  batch_size: 8192      
  lr: 3.0e-3            
  weight_decay: 1.0e-5
  l2_lambda: 1.0e-5
  num_neg: 1
  max_grad_norm: 1.0
  amp: true          
  patience: 5
  eval_every: 1
  eval_batch_size: 16384 
  num_workers: 8
  save_dir: "/content/checkpoints" 
  device: "cuda"

wandb:
  enabled: true
  project: "bagnn-recsys"
  entity: "nguyenmaiductrong37-h-c-vi-n-c-ng-ngh-b-u-ch-nh-vi-n-th-ng"
  run_name: "bagnn-g4"
  artifact_name: "bagnn-checkpoint"
  save_every: 5